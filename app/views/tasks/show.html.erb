
<h1><%= @task.title %></h1>
  <p><%= @task.details %></p>
  <p>
  <input type="checkbox"
  id="task"
  value='#{<%= @task.completed %>}'>
  <%= link_to @task.title, task_path(@task) %>
  </p>
  <%= link_to 'Back to list', tasks_path  %>
